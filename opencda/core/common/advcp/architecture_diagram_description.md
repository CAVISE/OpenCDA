# Пояснение к схеме архитектуры модуля AdvCP (текущая реализация)

## Общая концепция

Диаграмма показывает реальный поток данных в модуле AdvCP, основанный на методе `CoperceptionModelManager.make_prediction()`. В отличие от ранних версий, **нет зависимости от OPV2V dataset** — используются только встроенные датасеты OpenCOOD.

## Ключевые компоненты

### 🟦 **OpenCDA Core**

- **CoperceptionModelManager** — главный менеджер восприятия, который управляет всем процессом

### 🗄️ **Data Source**

- **DataLoader** — загрузчик батчей данных
- **OpenCOOD Dataset** — встроенные датасеты OpenCOOD (без внешних OPV2V)

### 🧠 **Perception Model**

- **OpenCOOD Model** — нейросеть для совместного восприятия
- **Inference Utils** — утилиты для инференса (early/intermediate/late fusion)

### ⚡ **AdvCP Module**

- **AdvCPManager** — центральный контроллер модуля (метод `process_tick()`)
- **Attacker** — компоненты атак (удаление/спуфинг LiDAR)
- **Defender** — компоненты защиты (CAD Defense)

### 📊 **Output & Evaluation**

- **Eval Utils** — расчет метрик (TP/FP)
- **Visualizer** — визуализация 3D/BEV

## Цветовая легенда потоков данных

| Цвет | Назначение | Пример |
| --- | --- | --- |
| **🟦 BLUE** | Обычный поток OpenCOOD | Инференс, загрузка данных |
| **🟩 GREEN** | Атака (включена) | Модификация данных атакой |
| **🟥 RED** | Защита (включена) | Проверка консистентности |
| **🟧 ORANGE** | Управление AdvCP | Решение о включении/отключении |
| **🟪 PURPLE** | Оценка и визуализация | Метрики, рендеринг |
| **--- DASHED** | Опциональные потоки | Визуализация (если включена) |
| **··· DOTTED** | Ссылки/инициализация | Веса модели |

## Последовательность работы (шаги)

```python
# Псевдокод основного потока
1. Инициализация:
   - CoperceptionModelManager создает DataLoader из OpenCOOD dataset
   - Загружает OpenCOOD модель
   - Инициализирует AdvCPManager (если включен)

2. Основной цикл (на каждый tick):
   - Получение batch_data из DataLoader
   - Сохранение batch_data в _current_batch_data (для AdvCP)
   - Оригинальный инференс через inference_utils

   # ТОЧКА ПРИНЯТИЯ РЕШЕНИЯ ADVCP
   if advcp_enabled:
       3. Передача контроля AdvCPManager (process_tick())

       4. Получение сырых данных (без рекурсии):
          - AdvCPManager вызывает _get_coperception_data()
          - CoperceptionModelManager возвращает _current_batch_data
          - Избегает circular dependency

       5. Атака (если включена):
          - Применение выбранного типа атаки
          - Возврат modified_data

       6. Защита (если включена):
          - Проверка консистентности через CAD Defense
          - Возврат defended_data и score

       7. Возврат результатов в CoperceptionModelManager
   else:
       # Использование оригинальных предсказаний

   8. Оценка метрик (TP/FP)

   9. Визуализация (опционально)
```

## Особенности текущей реализации

### ✅ **Устранены проблемы ранних версий:**

1. **Нет зависимости OPV2V** — используются только датасеты OpenCOOD
2. **Нет отдельной инициализации датасета** в AdvCP
3. **Прямая интеграция** в существующий пайплайн CoperceptionModelManager

###  **Условные ветвления:**

- Атака и защита работают **независимо** друг от друга
- Можно включить: только атаку, только защиту, или оба
- Результат защиты применяется **после** атаки (если обе включены)

### 📈 **Выходные данные:**

- `modified_data` — модифицированные предсказания (боксы, скоры)
- `score` — оценка доверия от защиты CAD
- `metrics` — дополнительные метрики для анализа