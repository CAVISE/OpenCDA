# Minimal dependencies to run the current unit tests fast in CI/act.
# Add here only what tests truly need.

cmake==4.4.2

lxml==6.1.2
# headless plotting
matplotlib==3.11.1

mypy-protobuf==5.1.0

networkx==3.6.1
ninja==1.13.0

# keep numpy pinned to avoid accidental numpy>=2 upgrades / binary incompat
numpy==2.5.2

# image I/O used by MLManager tests
opencv-python-headless==5.0.0.93
protobuf==7.36.0
pytest==9.1.1
pytest-mock==3.15.1
PyYAML==6.0.3
pyzmq==27.2.0

# used by KF/EKF tests
scipy==1.18.1

# geometry used by safety and map modules
shapely==2.1.2

# optional but often useful in logs/utilities
tqdm==4.70.0
traci==1.27.1
